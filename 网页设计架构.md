# Maple Precision Machinery 网页设计架构

## 一、整体架构设计

### 1.1 技术栈建议
- **前端框架**: React + TypeScript（或 Vue.js）
- **样式方案**: Tailwind CSS / CSS Modules
- **图片处理**: 响应式图片加载，支持懒加载
- **路由**: React Router（单页应用）
- **国际化**: react-i18next / i18next（英法双语支持）
- **构建工具**: Vite / Webpack

### 1.2 项目结构
```
MaplePrecisionMachinery/
├── public/
│   └── Product image/          # 图片资源（保持原位置或复制到public）
├── src/
│   ├── components/             # 可复用组件
│   │   ├── Header/
│   │   ├── Footer/
│   │   ├── ProductCard/
│   │   ├── ImageGallery/
│   │   ├── CategoryNav/
│   │   └── LanguageSwitcher/   # 语言切换组件
│   ├── pages/                  # 页面组件
│   │   ├── Home/
│   │   ├── Products/
│   │   ├── Category/
│   │   └── Contact/
│   ├── locales/                # 多语言资源文件
│   │   ├── en/                 # 英文翻译
│   │   │   ├── common.json
│   │   │   ├── navigation.json
│   │   │   ├── products.json
│   │   │   └── home.json
│   │   └── fr/                 # 法文翻译
│   │       ├── common.json
│   │       ├── navigation.json
│   │       ├── products.json
│   │       └── home.json
│   ├── data/                   # 数据配置
│   │   └── products.ts         # 产品分类和图片路径配置
│   ├── i18n/                   # 国际化配置
│   │   └── index.ts            # i18next 初始化配置
│   ├── styles/                 # 全局样式
│   ├── utils/                  # 工具函数
│   └── App.tsx                 # 主应用组件
└── package.json
```

## 二、页面结构设计

### 2.1 首页 (Home)
**布局结构:**
```
┌─────────────────────────────────────┐
│          Header (导航栏)              │
│  [Logo] [Home] [Products] [About] [Contact] [EN/FR] │
├─────────────────────────────────────┤
│                                     │
│      Hero Section (横幅区域)         │
│    - 公司Logo大图展示                │
│    - 公司简介文字                    │
│    - CTA按钮（查看产品）             │
│                                     │
├─────────────────────────────────────┤
│     产品分类预览 (4个卡片)            │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐│
│  │铸造件 │ │机械件 │ │船舶件 │ │拖车件 ││
│  │[图片] │ │[图片] │ │[图片] │ │[图片] ││
│  └──────┘ └──────┘ └──────┘ └──────┘│
├─────────────────────────────────────┤
│     公司优势/特色展示                 │
│  - 跨境贸易经验                      │
│  - 产品质量保证                      │
│  - 专业服务团队                      │
├─────────────────────────────────────┤
│          Footer (页脚)               │
│  联系信息 | 社交媒体 | 版权信息       │
└─────────────────────────────────────┘
```

### 2.2 产品列表页 (Products)
**布局结构:**
```
┌─────────────────────────────────────┐
│          Header                     │
├─────────────────────────────────────┤
│  面包屑导航: Home > Products         │
├─────────────────────────────────────┤
│  产品分类筛选栏                      │
│  [All] [Cast Parts] [Mechanical] [Ship] [Trailer]│
├─────────────────────────────────────┤
│  产品网格展示 (响应式)                │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐              │
│  │图│ │图│ │图│ │图│              │
│  │片│ │片│ │片│ │片│              │
│  │+ │ │+ │ │+ │ │+ │              │
│  │名│ │名│ │名│ │名│              │
│  └──┘ └──┘ └──┘ └──┘              │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐              │
│  │...│                              │
├─────────────────────────────────────┤
│          Footer                     │
└─────────────────────────────────────┘
```

### 2.3 产品分类详情页 (Category)
**布局结构:**
```
┌─────────────────────────────────────┐
│          Header                     │
├─────────────────────────────────────┤
│  面包屑: Home > Products > [Category Name]│
├─────────────────────────────────────┤
│  分类介绍区域                        │
│  - 分类描述文字                      │
│  - 代表性产品图片轮播                │
├─────────────────────────────────────┤
│  子分类导航（如果有）                 │
│  例如：船舶配件 >                    │
│    [船舶配件] [船舶钢材] [轴舵系统]   │
├─────────────────────────────────────┤
│  产品图片画廊                        │
│  - 网格布局或瀑布流                  │
│  - 点击放大查看                      │
│  - 支持图片懒加载                    │
├─────────────────────────────────────┤
│          Footer                     │
└─────────────────────────────────────┘
```

### 2.4 产品详情/图片查看页
**功能:**
- 图片灯箱/模态框展示
- 支持左右切换
- 支持缩放
- 显示产品名称

## 三、组件设计

### 3.1 Header 组件
**功能:**
- 公司Logo（链接到首页）
- 主导航菜单（根据当前语言显示）
- 响应式汉堡菜单（移动端）
- 语言切换器（EN/FR 切换按钮）
  - 显示当前语言
  - 点击切换语言
  - 保存用户语言偏好（localStorage）

### 3.2 ProductCard 组件
**属性:**
- `image`: 图片路径
- `title`: 产品名称
- `category`: 分类
- `onClick`: 点击事件

**样式:**
- 卡片式设计，带阴影
- 图片hover效果
- 响应式尺寸

### 3.3 ImageGallery 组件
**功能:**
- 网格布局展示多张图片
- 懒加载优化
- 点击图片打开灯箱
- 支持筛选和搜索

### 3.4 CategoryNav 组件
**功能:**
- 分类导航菜单（多语言支持）
- 高亮当前分类
- 下拉子菜单（如有子分类）

### 3.5 LanguageSwitcher 组件
**功能:**
- 语言切换按钮/下拉菜单
- 显示当前语言（EN/FR）
- 切换语言并更新整个应用
- 保存语言偏好到 localStorage
- 样式：按钮或下拉选择器

## 四、数据配置

### 4.1 产品分类配置
```typescript
interface ProductCategory {
  id: string;
  nameEn: string;      // 英文名称
  nameFr: string;      // 法文名称
  path: string;
  imagePath: string;
  descriptionEn?: string;  // 英文描述
  descriptionFr?: string;  // 法文描述
  subCategories?: SubCategory[];
}

interface SubCategory {
  id: string;
  nameEn: string;      // 英文名称
  nameFr: string;      // 法文名称
  path: string;
}
```

### 4.3 多语言翻译文件结构
**英文翻译文件 (locales/en/)**
```json
// common.json
{
  "welcome": "Welcome",
  "learnMore": "Learn More",
  "viewProducts": "View Products",
  "contactUs": "Contact Us"
}

// navigation.json
{
  "home": "Home",
  "products": "Products",
  "about": "About Us",
  "contact": "Contact"
}

// products.json
{
  "allProducts": "All Products",
  "castParts": "Cast and Forged Parts",
  "mechanical": "Mechanical Equipment and Parts",
  "shipFittings": "Ship Fittings",
  "trailerParts": "Trailer Parts"
}
```

**法文翻译文件 (locales/fr/)**
```json
// common.json
{
  "welcome": "Bienvenue",
  "learnMore": "En savoir plus",
  "viewProducts": "Voir les produits",
  "contactUs": "Nous contacter"
}

// navigation.json
{
  "home": "Accueil",
  "products": "Produits",
  "about": "À propos",
  "contact": "Contact"
}

// products.json
{
  "allProducts": "Tous les produits",
  "castParts": "Pièces moulées et forgées",
  "mechanical": "Équipements et pièces mécaniques",
  "shipFittings": "Accessoires maritimes",
  "trailerParts": "Pièces de remorque"
}
```

### 4.2 图片路径映射
- 自动扫描 `Product image/` 目录
- 生成图片路径配置
- 支持图片元数据（名称、描述）

## 五、样式设计规范

### 5.1 色彩方案
- **主色**: 红色（#DC2626，对应枫叶红）
- **辅色**: 深灰色（#374151，对应齿轮灰）
- **背景**: 白色/浅灰（#F9FAFB）
- **文字**: 深灰（#111827）

### 5.2 字体
- **标题**: 粗体无衬线字体（如 Inter Bold）
- **正文**: 常规无衬线字体（如 Inter Regular）
- **多语言**: 支持英文和法文字体渲染
- **字体回退**: 确保法文字符正确显示

### 5.3 响应式断点
- **移动端**: < 768px
- **平板**: 768px - 1024px
- **桌面**: > 1024px

## 六、功能特性

### 6.1 核心功能
1. ✅ 产品分类浏览
2. ✅ 图片画廊展示
3. ✅ 图片放大查看
4. ✅ 响应式设计
5. ✅ 图片懒加载
6. ✅ SEO优化
7. ✅ **英法双语切换**（核心功能）

### 6.2 增强功能（可选）
1. 图片搜索功能
2. 产品标签/筛选
3. 联系表单
4. 图片下载功能
5. 社交媒体分享
6. 语言自动检测（根据浏览器设置）

## 七、性能优化

1. **图片优化**
   - 使用 WebP 格式
   - 响应式图片（srcset）
   - 图片压缩
   - 懒加载

2. **代码优化**
   - 代码分割
   - 组件懒加载
   - 路由懒加载

3. **缓存策略**
   - 静态资源缓存
   - CDN部署

## 八、开发步骤建议

1. **第一阶段**: 基础框架搭建
   - 创建项目结构
   - 配置路由
   - 设计Header和Footer

2. **第二阶段**: 首页开发
   - Hero区域
   - 产品分类预览卡片
   - 响应式布局

3. **第三阶段**: 产品页面
   - 产品列表页
   - 分类详情页
   - 图片画廊组件

4. **第四阶段**: 优化和测试
   - 性能优化
   - 跨浏览器测试
   - 移动端适配

5. **第五阶段**: 部署
   - 构建生产版本
   - 部署到服务器/CDN

## 九、文件组织建议

### 图片资源处理
- 保持原 `Product image/` 目录结构
- 在构建时复制到 `public/` 目录
- 或使用静态资源服务器

### 数据文件
创建 `src/data/products.ts` 配置文件，映射所有产品分类和图片路径。

## 十、多语言实现方案

### 10.1 国际化库配置

**安装依赖:**
```bash
npm install react-i18next i18next i18next-browser-languagedetector
```

**i18n 初始化配置 (src/i18n/index.ts):**
```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

// 导入翻译资源
import enCommon from '../locales/en/common.json';
import enNavigation from '../locales/en/navigation.json';
import enProducts from '../locales/en/products.json';
import enHome from '../locales/en/home.json';

import frCommon from '../locales/fr/common.json';
import frNavigation from '../locales/fr/navigation.json';
import frProducts from '../locales/fr/products.json';
import frHome from '../locales/fr/home.json';

i18n
  .use(LanguageDetector)  // 自动检测浏览器语言
  .use(initReactI18next)
  .init({
    resources: {
      en: {
        common: enCommon,
        navigation: enNavigation,
        products: enProducts,
        home: enHome,
      },
      fr: {
        common: frCommon,
        navigation: frNavigation,
        products: frProducts,
        home: frHome,
      },
    },
    fallbackLng: 'en',  // 默认语言为英文
    defaultNS: 'common',
    interpolation: {
      escapeValue: false,  // React 已经转义
    },
    detection: {
      order: ['localStorage', 'navigator'],  // 优先使用保存的语言偏好
      caches: ['localStorage'],
    },
  });

export default i18n;
```

### 10.2 语言切换器组件实现

**LanguageSwitcher 组件 (src/components/LanguageSwitcher/index.tsx):**
```typescript
import { useTranslation } from 'react-i18next';
import { useState } from 'react';

const LanguageSwitcher = () => {
  const { i18n } = useTranslation();
  const [isOpen, setIsOpen] = useState(false);

  const languages = [
    { code: 'en', name: 'English', flag: '🇬🇧' },
    { code: 'fr', name: 'Français', flag: '🇫🇷' },
  ];

  const currentLanguage = languages.find(lang => lang.code === i18n.language) || languages[0];

  const changeLanguage = (langCode: string) => {
    i18n.changeLanguage(langCode);
    setIsOpen(false);
  };

  return (
    <div className="relative">
      <button
        onClick={() => setIsOpen(!isOpen)}
        className="flex items-center gap-2 px-3 py-2 rounded hover:bg-gray-100"
      >
        <span>{currentLanguage.flag}</span>
        <span>{currentLanguage.code.toUpperCase()}</span>
      </button>
      
      {isOpen && (
        <div className="absolute right-0 mt-2 bg-white border rounded shadow-lg">
          {languages.map((lang) => (
            <button
              key={lang.code}
              onClick={() => changeLanguage(lang.code)}
              className={`w-full text-left px-4 py-2 hover:bg-gray-100 flex items-center gap-2 ${
                i18n.language === lang.code ? 'bg-gray-50' : ''
              }`}
            >
              <span>{lang.flag}</span>
              <span>{lang.name}</span>
            </button>
          ))}
        </div>
      )}
    </div>
  );
};

export default LanguageSwitcher;
```

### 10.3 在组件中使用翻译

**示例：Header 组件**
```typescript
import { useTranslation } from 'react-i18next';
import LanguageSwitcher from '../LanguageSwitcher';

const Header = () => {
  const { t } = useTranslation('navigation');

  return (
    <header>
      <nav>
        <a href="/">{t('home')}</a>
        <a href="/products">{t('products')}</a>
        <a href="/about">{t('about')}</a>
        <a href="/contact">{t('contact')}</a>
        <LanguageSwitcher />
      </nav>
    </header>
  );
};
```

### 10.4 多语言路由处理

**路由配置建议:**
- 使用路径参数或查询参数保存语言状态
- 或使用 i18next 的 localStorage 自动保存
- URL 格式：`/products?lang=en` 或 `/en/products`（可选）

### 10.5 SEO 多语言优化

1. **HTML lang 属性**
   ```typescript
   <html lang={i18n.language}>
   ```

2. **hreflang 标签**
   ```html
   <link rel="alternate" hreflang="en" href="https://example.com/en" />
   <link rel="alternate" hreflang="fr" href="https://example.com/fr" />
   ```

3. **元数据多语言**
   - 根据当前语言动态设置 title 和 description

### 10.6 翻译内容管理

**需要翻译的内容包括:**
1. 导航菜单项
2. 页面标题和描述
3. 按钮文字
4. 产品分类名称
5. 公司介绍文本
6. 联系表单标签
7. 错误和提示信息
8. Footer 内容

**翻译文件组织:**
- 按功能模块拆分（common, navigation, products, home）
- 便于维护和更新
- 支持命名空间（namespace）管理

### 10.7 语言切换用户体验

1. **即时切换**: 切换语言后立即更新所有文本
2. **状态保持**: 语言偏好保存到 localStorage
3. **视觉反馈**: 当前语言高亮显示
4. **平滑过渡**: 切换时避免页面闪烁
5. **URL 同步**: （可选）URL 反映当前语言

### 10.8 开发工作流

1. **创建翻译文件**: 先创建英文版本作为基准
2. **标记待翻译文本**: 使用 `t()` 函数包装所有文本
3. **翻译法文版本**: 由专业翻译人员提供法文翻译
4. **测试验证**: 确保两种语言下界面正常显示
5. **字体测试**: 确保法文字符正确渲染
